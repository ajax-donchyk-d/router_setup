[project]
name = "router-setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dmytro", email = "donchyk.d@ajax.systems" }
]
requires-python = ">=3.14"
dependencies = [
    "colorlog>=6.12.0",
    "paramiko",
    "pydantic",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
router-setup = "router_setup:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
